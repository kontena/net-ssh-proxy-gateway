source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in net-ssh-proxy-tunnel.gemspec
gemspec

group :development do
  gem 'pry'
end
